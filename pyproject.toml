[project]
name = "bug-triage-env"
version = "0.1.0"
description = "AI-powered bug triage system"
requires-python = ">=3.10"
dependencies = [
    "gradio",
    "fastapi",
    "uvicorn",
    "openai",
    "openenv-core>=0.2.0",
]

[project.scripts]
server = "server.app:main"